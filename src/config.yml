data:
  train_path: data/train.csv
  test_path: data/test.csv

train:
  test_size: 0.2
  random_state: 42
  shuffle: true

model:
  name: GradientBoostingClassifier
  params:
    max_depth: null
    n_estimators: 10
  store_path: models/
